# IMPLEMENTATION_PLAN.md — Ralph state za bitlab-ai-asistent

Plan koji Ralph čita i ažurira. Bira top task iz Now, implementira, commit-uje, premješta u Done.

## Now

- **Acceptance: full 250 eval re-run, validate ≥95% PASS rate (REZUM 137→250)** — Multi-iter rezum loop u toku. Iter27 (commit ove iteracije) pomakao stable JSONL sa 121 na 137 verdicata (16 nova: 11 PASS + 5 FAIL, indexi 121-136 — cat-leaf-249..264 range). Cache hits 3/137 (cat-leaf-253 FAIL, cat-leaf-260 PASS, cat-parent-261 PASS — verdicti iz prethodnih runova sa istim prompt hash-om). Eval rezultat na 137: PASS 105 / FAIL 32 / WARN 0 / **76.6%** rate — mali pad sa iter26 77.7% (5 novih FAIL u batch-u od 16, batch rate 68.75% gore od overall). Trend: iter20=75.0% na 20, iter21=74.1% na 54, iter22=72.6% na 62, iter23=73.4% na 64, iter24=75.3% na 81, iter25=77.3% na 119, iter26=77.7% na 121, iter27=76.6% na 137 — stabilno u 75-78% range-u, daleko ispod 95% acceptance gate-a. Sljedeća iteracija (nakon što PAUSE marker `ralph/PAUSE` istekne — `until=1779799287` = **26.05.2026 14:41:27 CEST**) treba: `python -m evals.framework.runner --suite categories --mode full --label ralph-iter17-prompt-fix-final --resume ralph-iter17-prompt-fix-final`. Acceptance: stable JSONL ima 250 verdicata, PASS rate ≥95% (toleriše par WARN scenarij entry-ja per spec §5); ako rate < 95%, novi task u Next sa analizom rezidualnih FAIL-ova. Spec: `specs/categories.md` §5.

## Next
- **Group A3 fix: sinonim telefon/mobitel — `cat-leaf-186` "Dodaci za telefone" → search_products(176) ne (186)** — Rezidualni FAIL iz sample iter18 (28/30 = 93.3% acceptance ✓, ali ovaj entry još pada). Pattern: query "Dodaci za telefone" → model zvao `search_products(category_id=176)` što je "Dodaci za mobitele" (parent 151 "Mobiteli, GPS, dronovi") umjesto egzaktnog `search_products(186)` "Dodaci za telefone" (parent 154 "Telefoni — fiksni i bežični"). Model tretira "telefon" i "mobitel" kao sinonime u BS/SR/CG (što oni jesu u svakodnevnom govoru), ali eval set razlikuje fiksni/bežični telefon (154) od mobitela (151). Iter17 Done entry za cat-leaf-186 tvrdi ReadTimeout — iter18 sad pokazuje drukčiji pattern (search uspio, ali wrong ID). Fix opcija: (a) prompt dopuna sa eksplicitnim primjerom "Dodaci za telefone → 186 (NE 176; 'telefon' u katalogu = fiksni/bežični, ne mobitel)"; (b) ili premjestiti cat-leaf-186 u `categories_manual.jsonl` kao ambiguous_name jer realni korisnik ipak misli mobiteli kad kaže telefon u kontekstu webshop-a (kalibracija eval-a). Spec: `specs/categories.md` §4.
- **Group B2 fix rezidual: `cat-leaf-253` "Dodaci" još traži clarification uprkos fix #2** — Sample iter18 (28/30 = 93.3%): cat-leaf-253 query "Dodaci" → iters=1, tools=[], reply "Ima više kategorija 'Dodaci' u katalogu — za mobitele, notebook, tablet, fotoaparate, konzole, klime, ormare, monitore...". Prompt fix #2 ("OBAVEZNO PROBAJ TOOL ako ime postoji u mapping listi") trebao da eliminiše ovaj pattern, ali model još tretira "Dodaci" kao toliko ambiguous da ne pokušava ni tool poziv. Razlog vjerovatno: ime "Dodaci" se pojavljuje u mapping listi VIŠE PUTA (cat-leaf-253 + cat-leaf-176 "Dodaci za mobitele" + cat-leaf-186 "Dodaci za telefone" + možda još) — model vidi multi-match i tretira kao ambiguous trigger. Fix opcija: (a) prompt dopuna "ako se isto ime pojavljuje VIŠE PUTA u leaf mapping listi i query je samo to ime bez konteksta, biraj NAJMANJI category_id (najgenerišnu/najraniju leaf grupu) i pozovi tool — clarification SAMO ako nakon tool poziva rezultat prazan"; (b) ili premjesti cat-leaf-253 u `categories_manual.jsonl` kao ambiguous_name (eval kalibracija — slično otvorenom task-u za cat-leaf-112 Ventilatori). Pomenuto u iter17 Done entry kao primarni cilj fix #2; rezidual potvrđen u iter18 sample. Spec: `specs/categories.md` §4.
- **fix(eval): runner default `--label` = `--resume` value kad `--label` nije eksplicitno dat** — Trenutno argparse `--label` ima default `"adhoc"`, što znači da `--resume X` bez `--label X` napravi rezum READ iz `X.checkpoint` ali WRITE u `adhoc.checkpoint` i `adhoc.jsonl` — split state, stable JSONL ne nastavlja. Repro: iter9 24.05 23:30 pokrenuo `--resume ralph-iter8-pattern-analysis` bez `--label`, dobio `categories-adhoc.checkpoint.json` (sad obrisan). Fix u `evals/framework/runner.py:main()`: ako `args.resume is not None and args.label == "adhoc"`, postaviti `args.label = args.resume`. Unit test: parser dispatch verifikuje da resume-only invocation rezultira label==resume. Acceptance: spec komanda iz Now bez `--label` daje pravilan stable JSONL fajl.
- **Group C edge fix: Ventilatori clarification (cat-leaf-112)** — Per spec §4 ambiguous_name dopušta clarification, ali entry je u auto-gen `categories.jsonl` pa eval ga tretira kao striktan leaf match. Odluka: ili (a) premjesti cat-leaf-112 u `categories_manual.jsonl` kao ambiguous (eval kalibracija — najmanje invazivno), ili (b) prompt pravilo "za ambiguous leaf query gdje ime postoji u više leaf kategorija — biraj najgenerišnu i pozovi `search_products`; clarification samo ako 0 leaf match-eva". Provjeri da li ima drugih leaf entries sa istom dvosmislenošću (grep `data/categories_new.json` za istorodne nazive) prije odluke. Spec: `specs/categories.md` §4.
- **Group C edge fix: 3 ReadTimeout entries (cat-leaf-142, 186, 275)** — Eval client timeout već 120s (commit 5873704), ali 3 entries i dalje udaraju timeout — vjerovatno PWR cold-start na specifičnim queries. Investigacija: re-run samo te 3 sa `--no-cache --limit 3` (treba dodati id-filter argparse) i izmjeri elapsed_ms; ako > 60s konzistentno, dodaj retry logic u `evals/framework/client.py` (1 retry sa 30s backoff za ReadTimeout). Acceptance: 3 ReadTimeout entries PASS-uju u full eval ili dokumentovan kao known limit (≤5 entries iz 250, ispod 95% acceptance threshold-a).
- **Spec `specs/products.md` detaljan** — Trenutno placeholder. Treba popuniti: schema entry (`evals/sets/products.jsonl` šema), RAG izvor (`bck/app/rag.py` `load_index`/`search` cherry-pick lista), top-30 manualni entries iz `bck/var/bitlab.db` widget logova ili sintetički iz `data/products.meta.json`, judge.verdict_result kriterijumi (`min_results`, `top_result_contains_any`). Priprema za Fazu 2. Spec: `specs/products.md` (revidiraj).
- **Spec `specs/cross-reference.md` detaljan** — Trenutno placeholder. Treba popuniti: regex pattern price/brand (`do/ispod/iznad N KM`), poznata brand lista (`data/brands.json` ili extract iz `data/products.meta.json`), multi-step entries (`category_overview` → `search_products` sa parent_id), 20+ sample entries (`evals/sets/cross_reference.jsonl`). Priprema za Fazu 3. Spec: `specs/cross-reference.md`.
- **Regression set: snapshot prvih 10 PASS entry-ja iz Faze 1** — Kad Task #4 (acceptance full) zelena, kreiraj `evals/sets/regression_phase1.jsonl` sa 10 najstabilnijih PASS entry-ja (parent overview + leaf search miks). Cilj: nikad ne padaju u kasnijim Fazama 2/3. Acceptance: novi suite `--suite regression_phase1` u runner-u, dokumentovan u `ralph/AGENTS.md`.
- **Verdict cache cleanup utility** — Trenutno cache invalidacija je hash-based (mijenja prompt → novi hash → stari cache neaktivan, ne briše se). Dodaj `python -m evals.framework.runner --cache-prune --keep-last N` (ili `--cache-prune --older-than 7d`) da se cache folder ne nadima. Acceptance: cache folder size kontrolisan; CLI test pokriva prune behavior.
- **fix(ralph): `status.sh` SIGPIPE (exit 141) i nedostajuća `posljednji eval` sekcija** — Pre-existing pytest fail prije iter17 (potvrđeno stash test): `tests/integration/test_status_script.py` oba testa FAIL — `exit 141` (SIGPIPE) + output staje nakon IMPLEMENTATION_PLAN.md sekcije, prije `-- posljednji eval --`. Vjerovatno `head` pipe-uje u zatvoreni stdout u `IMPLEMENTATION_PLAN.md` sekciji jer je top Now task sad veliki blok teksta. Fix: u `ralph/status.sh` zamijeniti pipe `cat | head -N` sa `head -N <file>` ili `sed -n '1,N p'` koji ne baca SIGPIPE; verifikuj da je `-- posljednji eval` blok i dalje prisutan. Acceptance: oba testa u `test_status_script.py` PASS, backpressure 161 → 163 passed.

## Later

- **Faza 2**: RAG za `search_products` (cherry-pick iz `bck/app/rag.py`).
- **Faza 3**: cross-reference (multi-tool sekvence, filteri cijena/brand).
- **CI nightly eval**: GH Actions cron, real-LLM na staging.
- **Voice/email moduli**: out of scope za ovu inicijativu.

## Done

- 2026-05-26 PENDING_SHA Full eval iter27 rezum 121→137 (16 nova verdicta, pauza na budget gate). Komanda: `python -m evals.framework.runner --suite categories --mode full --label ralph-iter17-prompt-fix-final --resume ralph-iter17-prompt-fix-final`. PAUSE marker iz iter26 (do 26.05.2026 13:42:03 CEST, `until=1779795723`) istekao prije pokretanja; runner u resume grani učitao 121 prethodnih verdicata iz stable JSONL i nastavio od index 121. Obrađeno entries 121-136 (16 entries, cat-leaf-249..264 range). Cache hits 3/137 (cat-leaf-253 FAIL clarification, cat-leaf-260 PASS, cat-parent-261 PASS — verdicti iz prethodnih runova sa istim prompt hash-om). Eval rezultat na 137: PASS 105 / FAIL 32 / WARN 0 / **76.6%** rate. Budget gate ponovo udario na index 137 (PWR 52/52 u 5h prozoru, novi PAUSE marker do **26.05.2026 14:41:27 CEST**, `until=1779799287`). FAIL breakdown novih 5 instanci (entries 121-136, 2 distinkta pattern-a, oba pokrivena postojećim Next task-ovima): (a) **4× sub-B2 preventivna tehnička greška (iters=1, tools=[])** — cat-leaf-249 "Kuhinjski aparati" reply "Čini se da u kategoriji Kuhinjski aparati trenutno nema relevantnih proizvoda — rezultati koje v[raća]..." (varijanta: model halucinira da je vidio rezultate iako nikad nije pozvao tool), cat-leaf-252 reply "Nažalost, trenutno imam tehnički problem s pristupom katalogu", cat-leaf-256 reply "Nažalost, trenutno imam tehnički problem s pristupom katalogu", cat-leaf-262 reply "Nažalost, trenutno ne mogu dohvatiti katalog zbog tehničke greške s alatom". Sve 4 model preventivno odbacuje tool poziv uprkos prompt fix #2 OBAVEZNO PROBAJ TOOL pravilu — pokriven postojećim Next task-om za prompt fix #3; (b) **1× cache HIT za poznati FAIL** — cat-leaf-253 "Dodaci" clarification reply (postojeći Next task "Group B2 fix rezidual"). Pozitivni nalazi iter27 batch: 11/16 PASS (68.75% batch rate, najslabiji batch od iter22 — sub-B2 sve češći kako se eval polako približava kuhinjskim/uredskim leaf-ovima 250-264 range gdje su query stringovi vrlo generički); cat-leaf-263 PASS sa 3 iter-a + 2 search_products poziva (263 i 318) — model proširio search opseg za "Tepihi" iz leaf 263 ka srodnoj kategoriji 318. Trend stabilan pad: iter26=77.7% na 121, iter27=76.6% na 137 (-1.1pp). Sub-B2 pattern sve dominantniji (4 od 5 novih FAIL u iter27); ako trend ostaje do kraja rezum loop-a (250/250), acceptance grana "rate < 95% → novi Next task" će se aktivirati prvenstveno za prompt fix #3 (sub-B2 eliminacija). Backpressure (ruff format 47 unchanged, ruff check all passed, mypy 16 source files no issues, pytest -q **162 passed + 1 skipped**) zelena. Spec: `specs/categories.md` §5.

- 2026-05-26 288e027 Full eval iter26 rezum 119→121 (2 nova verdicta, pauza na budget gate). Komanda: `python -m evals.framework.runner --suite categories --mode full --label ralph-iter17-prompt-fix-final --resume ralph-iter17-prompt-fix-final`. PAUSE marker iz iter25 (do 26.05.2026 10:20:04 CEST, `until=1779783604`) istekao prije pokretanja; runner u resume grani učitao 119 prethodnih verdicata iz stable JSONL i nastavio od index 119. Obrađeno entries 119-120 (2 entries, cat-leaf-247 "Tosteri" + cat-leaf-248 "Električni grillovi"). Cache hits 0/121 (2 nova entry-ja oba prvi put obrađena). Eval rezultat na 121: PASS 94 / FAIL 27 / WARN 0 / **77.7%** rate. Budget gate ponovo udario na index 121 (PWR 52/52 u 5h prozoru, novi PAUSE marker do **26.05.2026 13:42:03 CEST**, `until=1779795723`). Iter26 batch (2 entries, 100.0% batch rate) bez novih FAIL-ova — oba PASS sa search_products direktan leaf rutiranjem: (a) cat-leaf-247 iters=2, tools=[search_products(247)], reply nabraja 10 Gorenje tostera (kapacitet 2 kriške, razlike po snazi); (b) cat-leaf-248 iters=2, tools=[search_products(248)], reply nabraja 7 TEFAL+GORENJE električnih grillova sa cijenama 131-285 KM. Mali batch (samo 2 entries) jer je PWR budget već bio pri kraju kad je marker istekao — 5h prozor je istekao tek za prvi poziv, drugi je opet udario gate. Trend stabilan: iter20=75.0% na 20, iter21=74.1% na 54, iter22=72.6% na 62, iter23=73.4% na 64, iter24=75.3% na 81, iter25=77.3% na 119, iter26=77.7% na 121 — rate u stabilnom 77% range-u kako batch entries (kuhinjski aparati) i dalje PASS-uju prompt fix #1+#2 rutiranje precizno. Acceptance grana "rate < 95% → novi Next task" će se aktivirati za prompt fix #3 (sub-B2 + halucinacija + Group A1 leaf vs parent collision ponovljeni) ili tool dispatch refaktor kad rezum loop dođe do 250/250. Backpressure (ruff format 47 unchanged, ruff check all passed, mypy 16 source files no issues, pytest -q **162 passed + 1 skipped**) zelena. Spec: `specs/categories.md` §5.

- 2026-05-26 a95beee Full eval iter25 rezum 81→119 (38 nova verdicata, pauza na budget gate). Komanda: `python -m evals.framework.runner --suite categories --mode full --label ralph-iter17-prompt-fix-final --resume ralph-iter17-prompt-fix-final`. PAUSE marker iz iter24 (do 26.05.2026 09:37:44 CEST, `until=1779781064`) istekao prije pokretanja; runner u resume grani učitao 81 prethodna verdicta iz stable JSONL i nastavio od index 81. Obrađeno entries 81-118 (38 entries, cat-parent-227..cat-leaf-245 range — najveći batch od početka rezum loop-a). Cache hits 1/119 (cat-parent-227 — postojeći verdict iz prethodnih runova sa istim prompt hash-om; entries 81-118 uglavnom nova teritorija). Eval rezultat na 119: PASS 92 / FAIL 27 / WARN 0 / **77.3%** rate. Budget gate ponovo udario na index 119 (PWR 52/52 u 5h prozoru, novi PAUSE marker do **26.05.2026 10:20:04 CEST**, `until=1779783604`). FAIL breakdown novih 7 instanci (entries 81-118, 3 distinkta pattern-a, svi pokriveni postojećim Next task-ovima): (a) **3× sub-B2 preventivna tehnička greška (tools=[])** — cat-leaf-205 "Nintendo" iters=1 reply "Nažalost, trenutno nemam mogućnost da pretražim katalog — alat za pretragu nije dostupan...", cat-leaf-210 "Dronovi" iters=1 reply "Izgleda da trenutno imam tehnički problem s pristupom katalogu...", cat-leaf-239 "Zamrzivači" iters=1 reply "Nažalost, trenutno imam tehnički problem s pristupom katalogu...". Model preventivno odbacuje tool poziv uprkos prompt fix #2 OBAVEZNO PROBAJ TOOL pravilu — pokriven postojećim Next task-om za prompt fix #3; (b) **3× pattern 2 halucinacija proizvoda (tools=[])** — cat-leaf-207 "Igrice" iters=1 nabraja "70 PS4 igrica" sa cijenama "29 KM — Ratchet and Clank, FIFA 21, Control..." bez tool poziva, cat-leaf-222 "Miševi i grafički tableti" iters=1 nabraja Logitech/Genius/HP mod-ele sa URL-ovima bitlab.ba, cat-leaf-224 "Monitori" iters=1 nabraja ASUS monitore "VA24EHF 24" FHD IPS 100Hz 219 KM..." sa konkretnim cijenama 219-3299 KM. Pokriven postojećim Next task-om za prompt fix #3 (ZABRANA halucinacije bullet već u prompt-u, ali nedovoljno efikasan); (c) **1× Group C edge clarification** — cat-leaf-245 "Ventilatori" reply "Ventilatori postoje u dvije kategorije — da li misliš na PC ventilatore (za računarska kućišta) ili kućne ventilatore...". Identičan poznati pattern iz cat-leaf-112 u Next-u — postojeći task "Group C edge fix: Ventilatori clarification" pokriva. Pozitivni nalazi iter25 batch: 31/38 PASS (81.6% batch rate) — najveći batch i najbolji batch rate od iter18; 31 leaf/parent entries u 227-245 range prošli prompt fix #1+#2 rutiranje precizno (cat-parent-227 cache HIT PASS, cat-leaf-228..238/240..244 svi PASS rutiranje). Trend raste: iter20=75.0% na 20, iter21=74.1% na 54, iter22=72.6% na 62, iter23=73.4% na 64, iter24=75.3% na 81, iter25=77.3% na 119 — rate prešao 75% prvi put u rezum loop-u, ali još daleko ispod 95% acceptance gate-a. Ako trend ostaje do kraja rezum loop-a (250/250), acceptance grana "rate < 95% → novi Next task" će se aktivirati za prompt fix #3 (sub-B2 + halucinacija ponovljeni) ili tool dispatch refaktor. Backpressure (ruff format 47 unchanged, ruff check all passed, mypy 16 source files no issues, pytest -q **162 passed + 1 skipped**) zelena. Spec: `specs/categories.md` §5.

- 2026-05-26 b722da7 Full eval iter24 rezum 64→81 (17 nova verdicata, pauza na budget gate). Komanda: `python -m evals.framework.runner --suite categories --mode full --label ralph-iter17-prompt-fix-final --resume ralph-iter17-prompt-fix-final`. PAUSE marker iz iter23 (do 26.05.2026 08:35:52 CEST, `until=1779777352`) istekao prije pokretanja; runner u resume grani učitao 64 prethodna verdicta iz stable JSONL i nastavio od index 64. Obrađeno entries 64-80 (17 entries, cat-leaf-185..204 range — eval set preskače 191/192/193 jer ne postoje u `data/categories_new.json`). Cache hits 4/17 (cat-leaf-186, 194, 195, 198 — postojeći verdicti iz prethodnih runova sa istim prompt hash-om). Eval rezultat na 81: PASS 61 / FAIL 20 / WARN 0 / **75.3%** rate. Budget gate ponovo udario na index 81 (PWR 52/52 u 5h prozoru, novi PAUSE marker do **26.05.2026 09:37:44 CEST**, `until=1779781064`). FAIL breakdown novih 3 instance (entries 64-80, sva 3 poznata pattern-a — nema novog Next task-a): (a) **1× Group A3 sinonim telefon/mobitel** — cat-leaf-186 "Dodaci za telefone" (cache HIT) iters=2, zvao `search_products(category_id=176)` "Dodaci za mobitele" umjesto egzaktnog 186 (postojeći Next task za prompt dopunu sa eksplicitnim primjerom telefon vs mobitel); (b) **1× sub-B2 preventivna tehnička greška (tools=[])** — cat-leaf-200 "LED sijalice" iters=1, tools=[], reply "Nažalost, trenutno imam tehničkih poteškoća s pristupom katalogu" (postojeći pattern 3 iz iter19/20/21/22 Done — model i dalje preventivno odbacuje tool poziv uprkos prompt fix #2 OBAVEZNO PROBAJ TOOL pravilu, pokriven postojećim Next task-om za prompt fix #3); (c) **1× Group A1 leaf→parent collision** — cat-leaf-201 "Baterije" iters=2, zvao `category_overview(category_id=160)` "Baterije i punjači" (parent) umjesto `search_products(201)` (leaf) — ime "Baterije" sadržano u parent imenu "Baterije i punjači", model bira parent uprkos prompt fix #1 LEAF PRIORITET pravilu (varijanta poznatog pattern-a iz iter21 cat-leaf-123 "Kablovi i adapteri" + iter21 cat-leaf-132 "UPS, stabilizatori"). Pozitivni nalazi iter24 batch: 14/17 PASS (82.4% batch rate) — najbolji batch rate od iter20; cat-leaf-194 "Navigacioni uređaji" sad PASS (cache HIT, iter17 sample FAIL prepoznat kao Group A varijanta — prompt fix #1 efikasan na njega); 13 novih leaf entries u 185-204 range (cat-leaf-185 "Notebook stalci", 187 "Skeneri", 188 "Štampači multifunkcijski", 189 "Fax aparati", 190 "Tinte i toneri", 195/196/197/198/199 GPS/navigacija entries, 202/203/204 baterije/punjači leaf-ovi) svi PASS rutiranje. Trend stabilan: iter20=75.0% na 20, iter21=74.1% na 54, iter22=72.6% na 62, iter23=73.4% na 64, iter24=75.3% na 81 — rate oscilira oko 73-75%, daleko ispod 95% acceptance gate-a. Ako trend ostaje do kraja rezum loop-a (250/250), acceptance grana "rate < 95% → novi Next task" će se aktivirati za prompt fix #3 (sub-B2 + Group A1 ponovljeni) ili tool dispatch refaktor. Backpressure (ruff format 47 unchanged, ruff check all passed, mypy 16 source files no issues, pytest -q **162 passed + 1 skipped**) zelena. Spec: `specs/categories.md` §5.

- 2026-05-26 c2717d8 Full eval iter23 rezum 62→64 (2 nova verdicta, pauza na budget gate). Komanda: `python -m evals.framework.runner --suite categories --mode full --label ralph-iter17-prompt-fix-final --resume ralph-iter17-prompt-fix-final`. PAUSE marker iz iter22 (do 26.05.2026 05:13:07 CEST, `until=1779765187`) istekao prije pokretanja; runner u resume grani učitao 62 prethodna verdicta iz stable JSONL i nastavio od index 62. Obrađeno entries 62-63 (cat-leaf-183 "Telefoni - fiksni", cat-leaf-184 "Bežični telefoni"): oba PASS sa pozivima `search_products(category_id=183)` i `search_products(category_id=184)`, iterations=2 svaki, elapsed ~64s drugi entry. Reply-ji halucinirani Panasonic katalog (model nabraja 10 KX-TG* modela sa cijenama 64-154 KM i bitlab.ba URL-ovima — handler stub vraća `{"products": []}` u Fazi 1, RAG dolazi u Fazi 2; routing PASS jer expect provjerava samo tool selection). Cache hits 0/64 (entries 62-63 prvi put obrađeni). Eval rezultat na 64: PASS 47 / FAIL 17 / WARN 0 / **73.4%** rate. Budget gate ponovo udario na index 64 (PWR 52/52 u 5h prozoru, novi PAUSE marker do **26.05.2026 08:35:52 CEST**, `until=1779777352`). Iter23 batch (2 entries) bez novih FAIL-ova — telefonija leaf-ovi prošli prompt fix #1+#2 rutiranje precizno. Trend stabilan: iter20=75.0% na 20, iter21=74.1% na 54, iter22=72.6% na 62, iter23=73.4% na 64 — oscilira oko 73-75%, daleko ispod 95% acceptance gate-a. Ako trend ostaje do kraja rezum loop-a (250/250), acceptance grana "rate < 95% → novi Next task" će se aktivirati za prompt fix #3 ili tool dispatch refaktor. Backpressure (ruff format 47 unchanged, ruff check all passed, mypy 16 source files no issues, pytest -q **162 passed + 1 skipped**) zelena. Spec: `specs/categories.md` §5.

- 2026-05-26 2c607bc Full eval iter22 rezum 54→62 (8 nova verdicta, pauza na budget gate). Komanda: `python -m evals.framework.runner --suite categories --mode full --label ralph-iter17-prompt-fix-final --resume ralph-iter17-prompt-fix-final`. PAUSE marker iz iter21 (do 26.05.2026 04:56:05 CEST, `until=1779764165`) istekao prije pokretanja; runner u resume grani učitao 54 prethodna verdicta iz stable JSONL i nastavio od index 54. Obrađeno entries 54-61 (cat-leaf-170..177, 8 entries). Cache hits 0/62 (novi run dir; entries 54-61 prvi put obrađeni — verdict cache iz prethodnih runova nije ih dotakao jer su unutar fresh ranga). Eval rezultat na 62: PASS 45 / FAIL 17 / WARN 0 / **72.6%** rate. Budget gate ponovo udario na index 62 (PWR 52/52 u 5h prozoru, novi PAUSE marker do **26.05.2026 05:13:07 CEST**, `until=1779765187`). FAIL breakdown novih 3 instance (entries 54-61, 3 distinkta pattern-a): (a) **1× pattern 1 (leaf→category_overview style reply, tools=[])** — cat-leaf-172 "Fotoaparati i camcorderi" iters=2, tools=[], reply "Kategorija 'Fotoaparati i camcorderi' sadrži tri potkategorije: Fotoaparati, Sportske kamere i dodaci, te Dodaci za foto..." (model tretira 172 kao parent uprkos tome što 172 jeste leaf — `Fotoaparati i camcorderi` ime kolidira sa parent ID 170 koji ima ista ili slična djeca; varijanta Group A1 leaf vs parent collision); (b) **1× pattern 3 sub-B2 "preventivna tehnička greška" (tools=[])** — cat-leaf-176 "Dodaci za mobitele" iters=1, tools=[], reply "Nažalost, trenutno ne mogu dohvatiti katalog — alat za pretragu nije dostupan..." (model preventivno odbacuje tool poziv); (c) **1× pattern 5 ReadTimeout** — cat-leaf-175 (postojeći Next task za retry/timeout logic). Trend stabilan: iter20 75% na 20, iter21 74.1% na 54, iter22 72.6% na 62 — rate polako pada kako se uzorak širi (svaki novi batch unosi 2-3 nova FAIL-a), FAIL pattern-i ravnomjerno raspoređeni. Prompt fix #2 i dalje nije efikasan na pattern 1 (leaf→overview) i pattern 3 (sub-B2 preventivna tehnička greška) na ovim entries. Ako trend ostaje do kraja rezum loop-a (250/250), acceptance grana "rate < 95% → novi Next task" će se aktivirati za prompt fix #3 ili tool dispatch refaktor. Backpressure (ruff format 47 unchanged, ruff check all passed, mypy 16 source files no issues, pytest -q **162 passed + 1 skipped**) zelena. Spec: `specs/categories.md` §5.

- 2026-05-26 fd8f1cb Full eval iter21 rezum 20→54 (34 nova verdicta, pauza na budget gate). Komanda: `python -m evals.framework.runner --suite categories --mode full --label ralph-iter17-prompt-fix-final --resume ralph-iter17-prompt-fix-final`. PAUSE marker iz iter20 (do 26.05.2026 03:41:24 CEST, `until=1779759684`) istekao prije pokretanja; runner u resume grani učitao 20 prethodnih verdicata iz stable JSONL i nastavio od index 20. Obrađeno entries 20-53 (cat-parent-94 .. cat-leaf-168, 34 entries). Cache hits 5/34 (cat-leaf-131, cat-parent-148, cat-parent-151, cat-parent-154, cat-parent-160 — verdict cache iz prethodnih runova sa istim prompt hash-om). Eval rezultat na 54: PASS 40 / FAIL 14 / WARN 0 / **74.1%** rate. Budget gate ponovo udario na index 54 (PWR 52/52 u 5h prozoru, novi PAUSE marker do **26.05.2026 04:56:05 CEST**, `until=1779764165`). FAIL breakdown novih 9 instanci (entries 20-53, 4 distinkta pattern-a): (a) **2× pattern 1 (leaf→category_overview style reply, tools=[])** — cat-leaf-123 "Kablovi i adapteri" nabraja "potkategorije: USB kablovi, Audio adapteri, Audio kablovi, Optički kablovi, Video kablovi, Video adapter, Napojni kablovi, Telefonski kablovi..." bez tool poziva (model tretira 123 kao parent), cat-leaf-132 "UPS, stabilizatori" sličan overview-style reply tools=[] ("nalaze se sljedeće potkategorije: UPS, Stabilizatori regulatori, Utičnice..."); (b) **3× pattern 2 halucinacija** — cat-leaf-124 "Laserski printeri" iters=?, tools=[], navodi 85 HP printera sa cijenama 209-2.699 KM i URL-ovima bitlab.ba; cat-leaf-128 "Kopir aparati" tools=[] nabraja 5 RICOH modela (IM 2702 MFP 3.159 KM, ...) sa URL-ovima; cat-leaf-167 "Rezervni dijelovi elektronika" tools=[] nabraja Samsung Galaxy S22 ekrane sa Original Quality oznakom i URL-ovima; (c) **3× pattern 3 sub-B2 "preventivna tehnička greška" (tools=[])** — cat-parent-149 "Projektori, prezentaciona oprema" reply "Nažalost, trenutno ne mogu dohvatiti podatke iz kataloga — servis nije dostupan", cat-parent-157 "Navigacioni uređaji" reply "Nažalost, trenutno nije moguće dohvatiti pregled kategorije zbog tehničke greške", cat-parent-158 "Klima uređaji" reply "Nažalost, trenutno ne mogu da dohvatim pregled kategorije Klima uređaji — alat nije dostupan" (tri uzastopna parent entries — moguć PWR backend degradation u tom batch-u koji je pojeo tool-call dispatch); (d) **1× pattern 5 ReadTimeout** — cat-leaf-168 (postojeći Next task za retry/timeout logic). Trend stabilan: iter20 75% na 20, iter21 74.1% na 54 — FAIL pattern-i ravnomjerno raspoređeni, prompt fix #2 i dalje nije efikasan na pattern 2 (halucinacija) i pattern 3 (sub-B2 preventivna tehnička greška). Ako trend ostaje do kraja rezum loop-a (250/250), acceptance grana "rate < 95% → novi Next task" će se aktivirati za prompt fix #3 ili tool dispatch refaktor. Backpressure ide u sljedećem koraku iteracije. Spec: `specs/categories.md` §5.

- 2026-05-26 b7b20ff Full eval iter20 rezum 5→20 (15 nova verdicta, pauza na budget gate). Komanda: `python -m evals.framework.runner --suite categories --mode full --label ralph-iter17-prompt-fix-final --resume ralph-iter17-prompt-fix-final`. PAUSE marker iz iter19 (do 26.05.2026 03:32:48 CEST) istekao prije pokretanja; runner u resume grani učitao 5 prethodnih verdicata iz stable JSONL i nastavio od index 5. Obrađeno entries 5-19 (cat-parent-94, 97, 107 + cat-leaf-93/94/97/101/108/111/112/117/120 + 2 dodatna parent/leaf). Cache hits 2/20 (cat-parent-107, cat-leaf-111 — prethodno verdicovani sa istim prompt hash-om). Eval rezultat na 20: PASS 15 / FAIL 5 / WARN 0 / **75.0%** rate. Budget gate ponovo udario na index 20 (PWR 52/52 u 5h prozoru, novi PAUSE marker do **26.05.2026 03:41:24 CEST**, `until=1779759684`). FAIL breakdown (5 instanci na 5-19 range, 4 distinkta pattern-a): (a) **3× pattern 2 halucinacija** — cat-leaf-101 "Produženje garancije" (iters=1, no tool, halucinira APC garantne listove sa cijenama 1999/2999 KM), cat-leaf-108 "Matične ploče" (iters=1, no tool, halucinira tabelu 22 modela ASUS/Gigabyte/MSI sa cijenama), cat-leaf-120 "Napojne jedinice" (iters=1, no tool, halucinira 12 napajanja FSP 80+ Bronze/Thermaltake sa cijenama); (b) **1× pattern 3 sub-B2 "tehnička greška"** — cat-leaf-117 "Graficke kartice" iters=1 reply "Trenutno ne mogu pristupiti katalogu proizvoda — alat za pretragu nije dostupan u ovom okruženju"; (c) **1× pattern 4 clarification** — cat-leaf-112 "Ventilatori" "PC vs kućanske ventilatore" (poznati Group C edge u Next). Pad sa 100% (iter19) na 75% (iter20) je signal da prompt fix #2 nije efikasan na pattern 2 — model i dalje halucinira proizvode sa konkretnim brendovima/cijenama bez ijednog tool poziva uprkos ZABRANA HALUCINACIJE bullet-u. Ako trend ostaje do kraja rezum loop-a (250/250), acceptance grana "rate < 95% → novi Next task" će se aktivirati za prompt fix #3 ili tool dispatch refaktor. Backpressure ide u sljedećem koraku iteracije. Spec: `specs/categories.md` §5.

- 2026-05-26 9102977 Full eval iter19 start — suite `ralph-iter17-prompt-fix-final` (finalna acceptance verifikacija Faze 1 nakon prompt fix-eva #1 i #2). Komanda: `python -m evals.framework.runner --suite categories --mode full --label ralph-iter17-prompt-fix-final`. Suite startovala čistom (nema postojećeg checkpoint-a / stable JSONL-a za taj label) i odmah pauzirala na index 5/250 zbog PWR budget gate-a (`52 poziva u 5h prozoru ≥ 52`, max_calls=80). Stable JSONL: PASS 5 / FAIL 0 / WARN 0 / **100.0%** (cat-parent-17, 94, 97, plus 2 nova). Cache hit-ovi prisutni na 3/5 entries — prompt hash nepromijenjen od iter18 fix #2, postojeći cache iz prethodnih runova (`categories-ralph-iter18-prompt-fix-B` i sample iter18) je preživio jer SHA cache key ostaje isti za nepromijenjeni `SYSTEM_PROMPT_V1` + tool shape. Run dir: `evals/runs/categories-ralph-iter17-prompt-fix-final-20260526-001316.html` + `categories-ralph-iter17-prompt-fix-final.jsonl` (5 redova). Checkpoint kreiran (`next_index=5, mode=full`); PAUSE marker `ralph/PAUSE` aktivan do **26.05.2026 03:32:48 CEST** (`until=1779759168`). Sample premali za acceptance ≥95% gate; multi-iter rezum loop ostaje aktivan kao Now task — sljedeća iteracija pokreće resume sa istim labelom kad marker istekne. Backpressure prije commit-a: ruff format 47 unchanged, ruff check all passed, mypy 16 source files no issues, pytest -q **162 passed + 1 skipped** (pre-existing `test_status_script.py` SIGPIPE FAIL iz iter17 dokumentacije više nije prisutan — vjerovatno fix prošao u međuvremenu; Next task `fix(ralph): status.sh SIGPIPE` može se evaluirati za skidanje u sljedećoj iteraciji ako se potvrdi green status). Spec: `specs/categories.md` §5.

- 2026-05-26 0828944 fix(eval): runner auto-detect `start_index` iz stable JSONL kad checkpoint nedostaje. `run_suite` (`evals/framework/runner.py`) sad u resume grani, ako `start_index == 0` i `resume_checkpoint.exists() == False`, čita `reporter.read_existing_verdicts(run_dir, suite_name, resume_label)` i postavlja `start_index = len(existing)` sa eksplicitnim printom `[resume] checkpoint nedostaje, auto-detected N iz stable JSONL`. Bool flag `auto_detected_from_jsonl` sprječava dvostruki print "krećem od index N (checkpoint <path>)" jer checkpoint NE postoji u tom scenariju (poruka bi bila misleading). Postojeći blok (linija ~199) koji učitava verdicte iz JSONL kada `start_index > 0` netaknut — pokriva oba puta (checkpoint-driven i auto-detect-driven resume). Novi integration test `test_runner_resume_auto_detects_start_index_from_stable_jsonl` u `tests/integration/test_eval_runner_resume.py`: pre-seed 5 PASS verdicata u stable JSONL bez checkpoint-a + suite od 10 entries → assertuje 5 novih `call_chat` poziva (indexes 5-9) i 10 ukupno redova u JSONL na kraju + `"auto-detected"` u stdout (capsys). Sva 4 postojeća resume testa (`missing_checkpoint_starts_from_zero`, `starts_from_checkpoint_index`, `resume_extends_same_jsonl`, `preserves_checkpoint_when_empty_range`) zelena — fix ne mijenja njihov ponašanje (uslov `not resume_checkpoint.exists()` triggers samo u novom scenariju). Backpressure (ruff format/check, mypy app/ evals/framework/ 16 source files, pytest -q 160 passed + 2 skipped) zelena. Pokriva iter10 dijagnozu: bez ovog fix-a Task #4 (full 250 acceptance) zahtijevao bi manual checkpoint kreiranje na svaku iteraciju gdje je marker zalutao.

- 2026-05-26 187b29b Sample eval `ralph-iter18-prompt-fix-B` rezum 22→30 (finalna validacija prompt fix #2, exit 0, suite kompletna). Komanda: `python -m evals.framework.runner --suite categories --mode sample --label ralph-iter18-prompt-fix-B --resume ralph-iter18-prompt-fix-B`. PAUSE marker iz prethodne iteracije (do 23:52:33 CEST 25.05) istekao prije pokretanja; runner obradio preostalih 8 entries (leaf stratum) i zatvorio suite sa exit 0, checkpoint obrisan automatski. Rezultat: PASS 28 / FAIL 2 / WARN 0 / **93.3%** (acceptance ≥93% ✓, 3pp iznad granice). FAIL breakdown (2 rezidualne instance, novi Next task-ovi): (a) **cat-leaf-186** "Dodaci za telefone" iters=2 → zvao `search_products(category_id=176)` "Dodaci za mobitele" umjesto egzaktnog 186 — semantička blizina telefon/mobitel sinonim (Group A3 varijanta, dopuna prompt fix #1); (b) **cat-leaf-253** "Dodaci" iters=1, tools=[] → clarification reply "Ima više kategorija 'Dodaci' u katalogu...", sub-B2 pattern koji prompt fix #2 nije uspio eliminisati (model vidi multi-match istog imena u mapping listi i tretira kao ambiguous trigger). Pozitivni nalazi: nijedan novi pattern nije otkriven u entries 22-29 izvan ova dva poznata; prompt fix #2 efikasan na 28/30 stratum entry-ja (uključujući cat-parent-277 "Ostalo" iz iter17 sample 30 FAIL-a — sada PASS); resume mehanika radi clean (verdict cache i auto-cleanup checkpoint-a po završetku suite-a). Cache hits 0/30 — sample stratum sa novim labelom, očekivano. Acceptance task-a (sample 30 verdicata, PASS ≥93%) **ISPUNJEN**. Backpressure ide u sljedećem koraku iteracije. Spec: `specs/categories.md` §5.

- 2026-05-25 75ae1f5 Sample eval `ralph-iter18-prompt-fix-B` rezum 7→22 (15 nova verdicta, pauza budget gate). Komanda: `python -m evals.framework.runner --suite categories --mode sample --label ralph-iter18-prompt-fix-B --resume ralph-iter18-prompt-fix-B`. PAUSE marker iz iter18 (do 23:17:53 CEST) istekao prije pokretanja; obrađeno entries 7-21 (15 entries iz stratum-a, ostatak sample 30). Eval rezultat na 22: PASS 22 / FAIL 0 / WARN 0 / **100.0%** rate. Budget gate ponovo udario na index 22 (PWR 52/52 u 5h prozoru, novi PAUSE marker do 23:52:33 CEST). Acceptance task-a (30 verdicata ≥93%) NIJE završen — preostalo 8 entries kroz novi rezum task u Now. Pozitivan signal: nijedan od poznatih fail pattern-ova iz iter17 sample 30 (Group A leaf→category_overview varijanta cat-leaf-194 + sub-B2 "tehnička greška" cat-leaf-253) nije ponovljen u entries 7-21 — prompt fix #2 (ZABRANA halucinacije + OBAVEZNO probaj tool) drži leaf routing preciznim i suzbija sub-B2 pattern. Backpressure prije commit-a: ruff format 47 unchanged, ruff check all passed, mypy 16 source files no issues, pytest 159 passed + 2 skipped.
- 2026-05-25 f9c4d9a Prompt fix #2 — Group B (ZABRANA halucinacije + OBAVEZNO probaj tool) implementiran u `app/agent.py:SYSTEM_PROMPT_V1`. Dva nova bullet pointa u "Pravila rutiranja": (a) **ZABRANA HALUCINACIJE** — STROGO ZABRANJENO nabrajati konkretan brend, model, cijenu (npr. 'Seasonic 450 KM', 'Target BB serija 82-115 KM') ili URL bez prethodnog uspješnog tool poziva u istoj konverzaciji; ako tool vrati prazan rezultat → 'trenutno nemam podatke za tu kategoriju' bez izmišljanja proizvoda; (b) **OBAVEZNO PROBAJ TOOL** — ako se ime kategorije iz upita pojavljuje u catalog mapping listi (parent ili leaf), OBAVEZNO pozovi tool prije reply-ja; ZABRANJENO odgovoriti 'tehnička greška' / 'nemam pristup katalogu' preventivno bez ijednog tool poziva — opravdano SAMO kad je prethodni tool poziv u istoj konverzaciji vratio exception ili error payload. Backpressure prošla (ruff format/check, mypy app/ evals/framework/, pytest 159 passed + 2 skipped); postojeći `test_system_prompt.py` 3 unit case-a (konstanta + oba toola + oba runnera) zelena jer shape nepromijenjen, samo dopuna sadržaja. Sample eval pokrenut sa labelom `ralph-iter18-prompt-fix-B`, pauziran budget gate-om na index 7/30 (PWR 52 poziva u 5h, PAUSE marker do 23:17:53 CEST); 7/7 PASS na parent stratum (cat-parent-17, 107, 151, 154, 277, 346, 347 — svi PASS routing, sve sa category_overview iters=2). cat-parent-277 "Ostalo" je u iter17 30-sample bio FAIL sa sub-B2 patternom ("tehnička greška"), sada PASS — pozitivan rani signal da fix #2 radi. Full statistička validacija ide kroz resume task (sample 30/30) i Task #3 (full 250 acceptance) u Now.

- 2026-05-25 3615e60 Sample eval `ralph-iter17-prompt-fix-A` rezum 10→30 (final 30 verdicta, exit 0). PAUSE marker istekao (do 22:26:33 CEST), obrađen preostali batch entries 10-29; checkpoint obrisan automatski, nema novog PAUSE markera. Rezultat: PASS 26 / FAIL 4 / WARN 0 / 86.7% (acceptance ≥90% **NIJE** ispunjen — 3.3pp ispod target-a). FAIL breakdown (4 instance): (a) cat-leaf-194 "Navigacioni uređaji" iters=2 → zvao `category_overview(194)` umjesto `search_products(194)`. **Group A varijanta**: pravi leaf ID ali pogrešan tool selection — različito od originalne Group A1 (parent ID umjesto leaf ID) koju prompt fix #1 cilja. Reply: "Kategorija Navigacioni uređaji ima tri potkategorije..." — handler `category_overview` izgleda da vraća djecu i za leaf, model interpretira kao clarification trigger. (b) cat-leaf-253 "Dodaci" iters=1 no tool → clarification reply ("Dodaci postoji za više kategorija..."), pattern sub-B2 iz prompt fix #2 task-a. (c) cat-leaf-186 + cat-leaf-324 ReadTimeout (poznati problem, postojeći Next task za retry/timeout). Group A coverage u stratified seed=42 sample: 1/12 entries (samo cat-leaf-194) — sample size premali za statističku validaciju Group A fix-a; full 250 eval (Task #4 u Now) je pravi test. Backpressure: pre-existing 2 FAIL `test_status_script.py` SIGPIPE (Next task `fix(ralph): status.sh SIGPIPE` dokumentovan), nije izazvan ovom iteracijom — potvrđeno commit history-jom (HEAD pre ove iter na 315bc0d, isti 2 FAIL bili tu).
- 2026-05-25 816e46e Prompt fix #1 — leaf routing prioritet (Group A) implementiran u `app/agent.py:SYSTEM_PROMPT_V1`. Dva nova bullet pointa u "Pravila rutiranja": (a) **LEAF PRIORITET** — ako se ime u upitu pojavljuje (egzaktno ili podstring) u LEAF mapping listi `search_products`, biraj `search_products(category_id=leaf_id)` čak i ako PARENT kategorija u `category_overview` listi ima identično ili slično ime; konkretni primjeri iz Group A1 (Kablovi, UPS, Televizori, USB uređaji, Konzole, Kućanski aparati, Fotoaparati) pokazuju leaf vs parent collision; (b) **TAČAN ID MATCH** — odaberi `category_id` koji odgovara TAČNO IMENU iz mapping liste, ne semantički blizak (Group A2 primjeri: Softver, Eksterni HDD, Kućišta za HDD). Backpressure prošla na izmijenjenom fajlu (ruff format/check, mypy, pytest unit `test_system_prompt_v1_*` 3 passed). 2 pre-existing FAIL u `tests/integration/test_status_script.py` (exit 141 SIGPIPE) nije izazvan od strane ovog task-a — potvrđeno `git stash` test pokretanjem na main commit-u 40e330b; dodat zaseban fix-up task u Next. Sample eval pokrenut sa labelom `ralph-iter17-prompt-fix-A`, pauziran budget gate-om na index 10/30 (PAUSE marker do 22:26:33 CEST); parcijalni rezultat: 10/10 PASS na svim parent-only stratum entries (cat-parent-*), Group A leaf entries dolaze u sljedećem batch-u. Acceptance (≥90% sample rate) nije validirana još — resume task u Now.
- 2026-05-25 c4f70b5 Suite kompletna na 250 verdicata (rezum 249→250, exit 0). iter16 pokrenula resume nakon što je PAUSE marker istekao (do 18:23:53 CEST), obradila preostali 1 entry (cat-leaf-396 "Mini PC") i zatvorila suite — runner exit 0, checkpoint obrisan automatski, nema novog PAUSE markera. Stable JSONL sada 250 verdicata (cijela `evals/sets/categories.jsonl` je 250 entries — acceptance u prethodnom Now task-u pomenuo 254 bio off-by-4, set u stvarnosti ima 250). Eval rezultat na 250: PASS 213 / FAIL 37 / WARN 0 / 85.2%. cat-leaf-396 PASS na routing (zvao `search_products(category_id=396)`); reply sadržava izmišljene "HYSTOU modele 539-1.069 KM" — pattern 2 iz Next (halucinacija proizvoda bez tool poziva), result=NA jer expect provjerava samo routing. Završni pattern breakdown FAIL 37: 19 leaf→category_overview (pattern 1), 8 halucinacija proizvoda (pattern 2), 6 iters=1 "tehnička greška" (pattern 3), 1 clarification (pattern 4), 3 ReadTimeout. Svi pokriveni postojećim Next task-ovima. Backpressure prošao prije commit-a (ruff format/check, mypy app/ evals/framework/, pytest -q 161 passed).
- 2026-05-25 784858a Resume eval pomak 235→249 (14 nova verdicta, pauza na budget gate). iter15 pokrenula resume nakon što je PAUSE marker istekao (do 18:11:16 CEST), obradila entries 235-248 (cat-leaf-380..395) i pauzirala se budget gate-om na index 249 (novi PAUSE marker do 18:23:53 CEST). Eval rezultat na 249: PASS 212 / FAIL 37 / WARN 0 / 85.1%. Novih FAIL-ova u 235-248 (1 instanca, pattern 2 "halucinacija proizvoda bez tool poziva" iz Next): cat-leaf-387 "Školski rančevi" → iters=1, tools=[], nabraja "Target BB seriju 82-115 KM" sa cijenama bez ijednog tool poziva. Ostalih 13 u 235-248 sva PASS. Preostalo do kraja suite: 5 entries (cat-leaf-396..). Backpressure prošao prije commit-a (ruff format/check, mypy app/ evals/framework/, pytest -q 161 passed).
- 2026-05-25 9c51f1a Resume eval pomak 208→235 (27 nova verdicta, pauza na budget gate). iter14 pokrenula resume nakon što je PAUSE marker istekao (do 17:19:28 CEST), obradila entries 208-234 (cat-leaf-344..370) i pauzirala se budget gate-om na index 235 (novi PAUSE marker do 18:11:16 CEST). Eval rezultat na 235: PASS 199 / FAIL 36 / WARN 0 / 84.7%. Novih FAIL-ova u 208-234 (3 instance, svi pattern 1 "leaf ime = parent ime collision" iz Next): cat-leaf-351 "USB uređaji" → `category_overview(347)` parent umjesto `search_products(351)` leaf; cat-leaf-360 "Kućanski aparati" → `category_overview` parenta "Kućanski aparati i bijela tehnika"; cat-leaf-368 "Konzole" → `category_overview` parenta "Konzole za igranje". Sva 3 model nabraja sub-kategorije iz overview-a i traži clarification. Pattern stabilan — pokriven postojećim Next fix-om. Backpressure prošao prije commit-a (ruff format/check, mypy app/ evals/framework/, pytest -q 161 passed).
- 2026-05-25 9111e0f Resume eval pomak 183→208 (25 nova verdicta, pauza na budget gate). iter13 pokrenula resume nakon što je PAUSE marker istekao (do 13:07:01 CEST), obradila entries 183-207 (cat-leaf-319..343) i pauzirala se budget gate-om na index 208 (novi PAUSE marker do 17:19:28 CEST). Eval rezultat na 208: PASS 175 / FAIL 33 / WARN 0 / 84.1%. Novih FAIL-ova u 183-207 (5 instanci): cat-leaf-320/325 (iters=1 NONE, halucinacija proizvoda sa cijenama i URL-ovima — pattern 2 iz Next), cat-leaf-327/329 (iters=2 search_products sa args=None pa halucinira Seagate/AXAGON brendove — moguć novi varijant: tool pozvan bez argumenata), cat-leaf-343 (iters=1 NONE "tehnička greška sa alatom za pretragu" — pattern 3 iz Next). Backpressure prošao prije commit-a (ruff format/check, mypy app/ evals/framework/, pytest -q 161 passed).
- 2026-05-25 6959478 Resume eval pomak 156→183 (27 nova verdicta, pauza na budget gate). iter12 pokrenula resume nakon što je PAUSE marker istekao (do 12:14:52 CEST), obradila entries 156-182 (cat-leaf-285..311) i pauzirala se budget gate-om na index 183 (novi PAUSE marker do 13:07:01 CEST). Eval rezultat na 183: PASS 155 / FAIL 28 / WARN 0 / 84.7%. Novih FAIL-ova u 156-182 (1 instanca): cat-leaf-304. Ostalih 27 FAIL = identičnih sa prethodnim checkpointom (104-155 i 0-103 range). Backpressure prošao prije commit-a (ruff format/check, mypy app/ evals/framework/, pytest -q 161 passed).
- 2026-05-25 793c2ef Resume eval pomak 104→156 (52 nova verdicta kroz dvije ralph iteracije). iter10 obradila entries 104-130 (cat-leaf-231..258) ali umrla prije commit-a; iter11 manual sync checkpoint (104→131) i nastavak 131-155 (cat-leaf-259..284). Eval rezultat na 156: PASS 129 / FAIL 27 / WARN 0 / 82.7%. Budget gate pauzirao na index 156 (52 PWR poziva u 5h prozoru, PAUSE marker do 25.05.2026 12:14:52). Novih FAIL-ova u 104-155 (9 instanci): 5 iter10 (cat-leaf-245 Ventilatori clarification, 246 Kuhala iters=1, 248/251 tehnička greška, 253 Dodaci clarification) + 4 iter11 (cat-leaf-270 Switchevi hallucinated catalog iters=1, 274 Softver wrong category_id 345 umjesto 274, 275 ReadTimeout, cat-parent-277 Ostalo tehnička greška). Pattern: 8/9 poznati uzorci iz Next sekcije + 1 novi (wrong category_id na cat-leaf-274 — model zvao search_products ali sa pogrešnim leaf id-om).
- 2026-05-25 84ace7f Resume iter8 eval (od 52 → pauziran na 104). Komanda: `python -m evals.framework.runner --suite categories --mode full --label ralph-iter8-pattern-analysis --resume ralph-iter8-pattern-analysis`. Dodano 52 nova verdicata u stable JSONL (`categories-ralph-iter8-pattern-analysis.jsonl`, sad 104 redova). Eval rezultat: PASS 86 / FAIL 18 / WARN 0 / 82.7% rate. Novi FAIL-ovi (52-103 range): 9 instanci — cat-leaf-142, 169, 171, 172, 186, 194, 201, 209, 225. Pattern: ponavljaju dva poznata (a) `iters=1` + `tool=NONE` ("tehnička greška" reply), (b) `category_overview` umjesto `search_products` na leaf. Plus 2 `ReadTimeout` (cat-leaf-142, 186 — 120s timeout iz prošlog commit-a 5873704). Budget gate pauzirao na index 104 (52 calls u 5h prozoru). PAUSE marker postavljen sa `until=1779659044` ali u prošlosti — otkrio bug u `_estimate_reset_epoch` (gleda global oldest u logu, ne oldest-in-window). Bug postavljen kao BLOCKER u Now za sljedeću iter prije nego što se rezum nastavi (inače ralph.sh wait_pause izbriše stari marker, runner odmah opet pauzira, infinite loop).
- 2026-05-24 f72e18a Fix: checkpoint/resume mode-mismatch handling. `_write_checkpoint` snima `mode` polje ("full"/"sample"); `_read_checkpoint` vraća dict `{next_index, mode}` (legacy bez polja → `mode=None`, backward compat). Resume grana u `run_suite` provjeri mismatch (`checkpoint.mode != args.mode`) i abort-uje exit 4 sa porukom "checkpoint je iz '<X>' mode, pokreni opet sa --mode <X> ili novi --label". Cleanup grana sad ima defensive guard `start_index < len(entries)` — prazno preskočili NE briši state. RED testovi (3 nova u `tests/integration/test_eval_runner_resume.py`): `test_checkpoint_includes_mode_field` (unit za `_write_checkpoint`), `test_runner_aborts_on_mode_mismatch` (integration), `test_runner_preserves_checkpoint_when_empty_range` (integration sa legacy checkpoint). Backpressure: 153 passed, 0 failed. Exit kod 4 dokumentovan u `runner.py` docstring.
- 2026-05-24 e0871a1 Analiza fail patterns nakon v1 baseline (iter8). Pokrenut `python -m evals.framework.runner --suite categories --label ralph-iter8-pattern-analysis` kroz PWR sa SYSTEM_PROMPT_V1 (commit 59ce28c). Budget gate pauzirao na index 52/250 (PWR pragom 65% × 80 = 52); kreiran PAUSE marker + checkpoint za resume u sljedećoj iteraciji. Rezultat na 52 entries: PASS 42 (80.8%), FAIL 10. Realnih FAIL-ova (ne-server): 9 + 1 ReadTimeout. Klasifikacija u 4 fail pattern grupa: (1) **leaf → category_overview kad parent ima slično ime** — 3 entries (Kablovi, UPS, Televizori); (2) **halucinacija konkretnih proizvoda bez tool poziva** — 2 entries (Napojne jedinice navodi Seasonic/Gembird/cijene; Zvucne kartice navodi HQ-Tech/CMedia/URL-ove); (3) **iters=1 + "tehnička greška" reply bez tool poziva** — 3 entries (Graficke kartice, Laserski printeri, Audio video parent); (4) **clarification umjesto search_products** — 1 entry (Ventilatori — PC vs kućanski vs USB). 4 targeted fix task-ova dodato u Next. Output JSONL: `evals/runs/categories-ralph-iter8-pattern-analysis-20260524-191020.jsonl`. Resume task za nastavak runa u Now.
- 2026-05-24 59ce28c `evals/sets/categories_manual.jsonl` negativni set (16 entries, expect.tool=null, 4×4: `not_in_catalog`/`ambiguous_name`/`typo_likely`/`out_of_scope`). SYSTEM_PROMPT_V1 dopuna typo guard bullet-om (model NE korigovaše typo automatski — pita clarification; spec § 4 dopušta tu granu). Eval kroz PWR (label `ralph-iter6-postfix`): cat-manual full 16 → PASS 15 / FAIL 1 → 93.8% (acceptance ≥80% ✓); categories sanity (limit 20/250) → PASS 19 / FAIL 1 → 95% (acceptance ≥50% ✓). Jedini cat-manual FAIL: `cat-manual-amb-baterija` → model dao `category_overview(160)` "Baterije i punjači" parent (spec § 4 dopušta overview za jasnu parent, ali eval framework strogi tool=null; entry ostavljen, eventualni refinement u sljedećoj iteraciji).
- 2026-05-24 3cdaa99 SYSTEM_PROMPT_V1 konstanta u `app/agent.py` (single source) prosljeđena u OBA runnera (`_run_anthropic` system= + `_run_pwr` prva poruka). Parent/leaf routing pravila + out-of-scope instrukcije (knjige, vrijeme, garancija → ne zovi tool). Unit testovi (test_system_prompt.py): konstanta nepraznja + imenuje oba toola. Recovery commit (Ralph iter 4 prekinut login expiry-jem prije commit-a; rad kompletan i kvalitetan, manualno commit-ovan).
- 2026-05-24 5873704 `search_products` tool u OBA runnera (Anthropic shape + OpenAI derivacija) sa query/category_id/brand/min_price_km/max_price_km poljima. Handler stub vraća `{"products": []}` (RAG u Fazi 2). Mapping splittan u `_parent_block()` (36 parent ID) i `_leaf_block()` (220 leaf ID) — model za leaf upit više ne bira category_overview. Integration testovi za oba backenda + handler unit test. Eval client timeout 30s → 120s (PWR cold-start). Live eval (limit 2 fail-fast, kroz PWR): `cat-parent-17` PASS (category_overview/17) + `cat-leaf-93` PASS (search_products/93). Rate 100%.
- 2026-05-24 087861a `category_overview` tool u OBA runnera (`_run_anthropic` Anthropic shape + `_run_pwr` OpenAI shape derivacija). Handler stub vraća djecu iz `data/categories_new.json`. Tool dispatch loop + `app/tools.py` modul. Integration test sa mock_llm × oba backenda. Live eval (limit 1, kroz PWR) — prvi parent entry `cat-parent-17` "Računari" → PASS 100%.
- 2026-05-24 3d928f6 Generiši auto-gen kategorija eval set (250 entry-ja: 220 leaves + 30 parents; deterministička skripta, schema + determinizam unit testovi)
